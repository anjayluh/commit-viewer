export interface INote {
    id: string,
    dateCreated?: Date,
    title: string,
    description: string,
}
export interface INoteDetails {
    id: string,
    dateCreated?: Date,
    title: string,
    description: string,
}